
//-- git hub 연동 후 작업 순서 --//
1. 본인이 작업할 개인 브랜치 생성
   (git branch 브랜치명) -> 브랜치명은 mjh
2. 생성한 개인 브랜치로 이동
   (git checkout 이동할 브랜치명)
3. 코딩한다.
4. 코딩한 내용을 스테이징한다. (git add .)
4. 커밋을 실행해서 지금까지 코딩한 내용에 대한 복원시점을 만든다 (git commit -m "커밋메세지)
5. main 브랜치 이동
  git checkout main
6. 다른 팀원이 먼저 올린 파일을 내려받기 
  git pull origin main
  * 위의 명령어 실행 시 이상한 화면으로 이동
   ->:exit 엔터
7. main 브랜치로 개인브랜치에서 작성한 내용을 main브랜치에 병합 (git merge 브랜치명)
8. 이상이 없으면 main 브랜치를 팀원에게 공유한다.
  git push origin main
9. 다음 작업을 하기위해 개인브랜치로 이동
10. 다른 팀원들이 공유한 내용을 개인브랜치에 받아 최신화 시킨다.
   git merge main
11. 최신화된 개인브랜치를 공유한다.
   git push origin 개인브랜치명(mjh)
12. 3번부터 11번까지 반복~~

//////////////////////////////////////////////////////////////////////
자바 들어오자마자 다른사람이 공유한파일 받기
----------------------------------git pull origin main
----------------------------------git merge 개인브랜치명
개인브랜치로 이동
----------------------------------git checkout 개인브랜치명
코딩후
----------------------------------git add .
----------------------------------git commit -m "메세지"
메인브랜치 이동
----------------------------------git checkout main
다른사람이 공유한파일 받기
----------------------------------git pull origin main
개인브랜치에서 코딩한내용 메인브랜치로 병합
----------------------------------git merge 개인브랜치명
다른사람에게 공유
----------------------------------git push origin main
개인브랜치 이동 후 개인브랜치 최신화
----------------------------------git merge main